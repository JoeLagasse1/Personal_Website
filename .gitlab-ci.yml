pages:
  stage: deploy
  script:
    - mkdir -p public
    - cp index.html public/
    - cp headshot.jpg public/
    - cp -r css public/
    - cp -r js public/
    - cp -r images public/
  artifacts:
    paths:
      - public
  rules:
    - if: $CI_COMMIT_BRANCH == "main"
